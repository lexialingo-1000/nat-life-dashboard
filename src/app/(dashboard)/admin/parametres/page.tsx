import { PlaceholderPage } from '@/components/placeholder-page';

export default function ParametresPage() {
  return (
    <PlaceholderPage
      title="Paramètres"
      description="Configuration globale du dashboard : email factures, paramètres MinIO, intégrations Pennylane (V1.5), Telegram (V1.5), n8n webhooks."
      lot="V1.5"
    />
  );
}
